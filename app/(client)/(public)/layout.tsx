export const metadata = {
  title: 'CA Chatroom',
  description: 'CA chatroom app',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
